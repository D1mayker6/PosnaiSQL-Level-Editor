<UserControl x:Class="PosnaiSQLauncher.NewMainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Opacity="0">
    
    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" From="0" To="1" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <!-- Уменьшил внешний Margin с 40 до 20, чтобы дать больше места контенту -->
    <Border CornerRadius="20"
            Background="#E8FFFFFF"
            Margin="20" 
            VerticalAlignment="Center"
            HorizontalAlignment="Center">
        <Border.Effect>
            <DropShadowEffect ShadowDepth="8" Color="#000000" Opacity="0.25" BlurRadius="16"/>
        </Border.Effect>

        <StackPanel Margin="30,10,30,25"> <!-- Немного уменьшил верхний отступ -->
            
            <!-- Уменьшил высоту логотипа с 200 до 150 - это даст 50px для новой кнопки -->
            <Image Width="300" Height="150" 
                   Source="pack://application:,,,/Assets/logo.png"
                   Stretch="Uniform"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"
                   RenderOptions.BitmapScalingMode="HighQuality"/>

            <!-- Название -->
            <TextBlock Text="Добро пожаловать!"
                      FontSize="26" 
                      FontWeight="Bold"
                      Foreground="#1a1a1a"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,5"/>

            <TextBlock Text="Редактор уровней"
                      FontSize="15"
                      Foreground="#1E88E5"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,2"/>

            <TextBlock Text="«SQL - Твой запрос на мастерство!»"
                      FontSize="11"
                      Foreground="#888888"
                      HorizontalAlignment="Center"
                      Margin="0,0,0,15"/> 

            <!-- Кнопки -->
            <Grid HorizontalAlignment="Center">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/> <!-- Уменьшил межкнопочный интервал с 15 до 10 -->
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="10"/> 
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Button Grid.Row="0"
                        Content="✚  Создать новый вариант"
                        Style="{StaticResource PrimaryButtonStyle}"
                        Height="55" 
                        MinWidth="350"
                        Click="CreateButton_Click"/>

                <Button Grid.Row="2"
                        Content="✎  Редактировать существующий вариант"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Height="55"
                        MinWidth="350"
                        Click="EditButton_Click"/>

                <Button Grid.Row="4"
                        Content="📩  Экспорт вариантов"
                        Style="{StaticResource SecondaryButtonStyle}"
                        Height="55"
                        MinWidth="350"
                        Click="ExportButton_Click"/>
            </Grid>
        </StackPanel>
    </Border>
</UserControl>