<UserControl x:Class="PosnaiSQLauncher.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Opacity="0">

    <UserControl.Resources>
        <Style x:Key="ModernTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border x:Name="border"
                                Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="15"/>
            <Setter Property="Padding" Value="15,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#E0E0E0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border x:Name="border"
                                Background="White"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ScrollViewer x:Name="PART_ContentHost"
                                        Margin="{TemplateBinding Padding}"
                                        VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                            </Trigger>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#2196F3"/>
                                <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="IconButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="#2196F3"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="20"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid RenderTransformOrigin="0.5,0.5">
                            <Grid.RenderTransform>
                                <ScaleTransform x:Name="scaleTransform" ScaleX="1" ScaleY="1"/>
                            </Grid.RenderTransform>
                            <Border x:Name="border"
                                    Background="{TemplateBinding Background}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    CornerRadius="8">
                                <ContentPresenter HorizontalAlignment="Center"
                                                VerticalAlignment="Center"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <EventTrigger RoutedEvent="MouseEnter">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1.05" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1.05" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <EventTrigger RoutedEvent="MouseLeave">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleX"
                                                       To="1" Duration="0:0:0.15"/>
                                        <DoubleAnimation Storyboard.TargetName="scaleTransform" 
                                                       Storyboard.TargetProperty="ScaleY"
                                                       To="1" Duration="0:0:0.15"/>
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#E3F2FD"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect ShadowDepth="4" Color="#2196F3" Opacity="0.3" BlurRadius="8"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#BBDEFB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <UserControl.Triggers>
        <EventTrigger RoutedEvent="Loaded">
            <BeginStoryboard>
                <Storyboard>
                    <DoubleAnimation Storyboard.TargetProperty="Opacity" 
                                   From="0" To="1" Duration="0:0:0.4"/>
                </Storyboard>
            </BeginStoryboard>
        </EventTrigger>
    </UserControl.Triggers>

    <Grid>
        <Border CornerRadius="20" 
                Background="#E8FFFFFF" 
                Margin="20" 
                Width="700" 
                VerticalAlignment="Center" 
                HorizontalAlignment="Center">
            <Border.Effect>
                <DropShadowEffect ShadowDepth="8" 
                                Color="Black" 
                                Opacity="0.2" 
                                BlurRadius="15"/>
            </Border.Effect>

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Row="0" 
                        Background="White" 
                        BorderBrush="#EEE" 
                        BorderThickness="0,0,0,1" 
                        Padding="30,25" 
                        CornerRadius="20,20,0,0">
                    <Grid>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <TextBlock Text="⚙️" 
                                     FontSize="28" 
                                     Margin="0,0,10,0" 
                                     VerticalAlignment="Center"/>
                            <TextBlock Text="Настройки" 
                                     FontSize="24" 
                                     FontWeight="Bold" 
                                     VerticalAlignment="Center"/>
                        </StackPanel>
                    </Grid>
                </Border>

                <ScrollViewer Grid.Row="1" 
                            VerticalScrollBarVisibility="Auto" 
                            Padding="30,25">
                    <StackPanel>
                        <TextBlock Text="Google Таблица" 
                                 FontSize="16" 
                                 FontWeight="SemiBold" 
                                 Margin="0,0,0,15"/>

                        <Grid Margin="0,0,12,10">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="10"/> <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="10"/> <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="GoogleSheetUrlTextBox"
                                     Grid.Column="0"
                                     Style="{StaticResource ModernTextBox}"/>

                            <Button Grid.Column="2"
                                    Style="{StaticResource IconButtonStyle}"
                                    Content="📋"
                                    ToolTip="Скопировать ссылку"
                                    Click="CopyUrl_Click"/>

                            <Button Grid.Column="4"
                                    Style="{StaticResource IconButtonStyle}"
                                    Content="🌐"
                                    ToolTip="Открыть в браузере"
                                    Click="OpenUrl_Click"/>
                        </Grid>

                        <TextBlock x:Name="ValidationMessage"
                                 FontSize="12"
                                 Margin="0,8,0,15"
                                 TextWrapping="Wrap"
                                 Visibility="Collapsed"/>

                        <Grid Margin="0,0,0,30">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="15"/>
                                <ColumnDefinition Width="Auto" MinWidth="180"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0"
                                    Background="#E3F2FD" 
                                    CornerRadius="10" 
                                    Padding="15">
                                <StackPanel>
                                    <TextBlock Text="ℹ️ Таблица результатов" 
                                               FontSize="14" 
                                               FontWeight="SemiBold" 
                                               Margin="0,0,0,0"/>
                                    <StackPanel>
                                        <TextBlock Text="Google Таблица используется для хранения результатов игроков." 
                                                   FontSize="10" 
                                                   Foreground="#555555" 
                                                   TextWrapping="Wrap"
                                                   Margin="0,0,0,5"/>
                                        <TextBlock Text="Ссылка должна быть на Google Table с правами редактора." 
                                                   FontSize="10" 
                                                   Foreground="#555555" 
                                                   TextWrapping="Wrap"/>
                                    </StackPanel>
                                </StackPanel>
                            </Border>

                            <Button Grid.Column="2"
                                    Content="💾 Сохранить" 
                                    Style="{StaticResource PrimaryButtonStyle}" 
                                    Click="SaveUrl_Click" 
                                    Padding="20,15"
                                    FontSize="20"
                                    Height="Auto"
                                    VerticalAlignment="Stretch"/>
                        </Grid>

                        <TextBlock Text="Безопасность" 
                                   FontSize="16" 
                                   FontWeight="SemiBold" 
                                   Margin="0,20,0,10"/>

                        <Button Content="🔐 Изменить пароль" 
                                Style="{StaticResource PrimaryButtonStyle}" 
                                Click="ChangePassword_Click" 
                                HorizontalAlignment="Left"
                                Margin="12,0,0,0"
                                Padding="20,15"
                                FontSize="14"/>

                        <Border Background="#FFF3E0" 
                              CornerRadius="10" 
                              Padding="15" 
                              Margin="0,15,0,0">
                            <StackPanel>
                                <TextBlock Text="⚠️ Важно" 
                                         FontSize="14" 
                                         FontWeight="SemiBold" 
                                         Margin="0,0,0,8"
                                         Foreground="#E65100"/>
                                <TextBlock Text="Используйте надежный пароль, содержащий буквы, цифры и символы." 
                                         FontSize="12" 
                                         Foreground="#555555" 
                                         TextWrapping="Wrap"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Row="2" 
                        Background="White" 
                        BorderBrush="#EEE" 
                        BorderThickness="0,1,0,0" 
                        Padding="30,15,30,12" 
                        CornerRadius="0,0,20,20">
                    <StackPanel>
                        <Button Content="Назад" 
                                Style="{StaticResource SecondaryButtonStyle}" 
                                Click="Back_Click" 
                                HorizontalAlignment="Stretch" 
                                MinWidth="0" 
                                Height="50"/>
                        
                        <TextBlock Text="Игровой программный комплекс «ПознайSQL. Редактор уровней», разработчик: Каланда Дмитрий, 2026" 
                                   FontSize="11" 
                                   Foreground="#999999" 
                                   HorizontalAlignment="Center" 
                                   Margin="0,12,0,0"
                                   FontWeight="Normal"/>
                    </StackPanel>
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>